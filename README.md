# Flax Rendering Pipeline Library
The library part of my FlaxEngine Rendering Pipeline. 

It's recommended to simply add this repository as a *submodule* to `Source/RenderingPipeline` if you want to use it.

